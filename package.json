{
  "name": "chapter_4",
  "version": "1.0.0",
  "description": "A dockerized Full stack todo application that uses NodeJS backend, a PostgreSQL database, a PRISMA ORM and JWT authentication",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "migrate": "^2.1.0",
    "pg": "^8.15.5",
    "prisma": "^6.6.0"
  }
}
